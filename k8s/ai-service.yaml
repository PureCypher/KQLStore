apiVersion: v1
kind: Service
metadata:
  name: kqlstore-ai
  namespace: kqlstore
  labels:
    app: kqlstore-ai
    app.kubernetes.io/managed-by: kustomize
spec:
  type: ClusterIP
  selector:
    app: kqlstore-ai
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
