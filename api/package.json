{
  "name": "kqlstore-api",
  "version": "1.0.0",
  "description": "KQL Store API server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "// engines": "better-sqlite3 11.x ships prebuilds for Node 18/20/22 only; newer runtimes fall back to a node-gyp build that fails. The Dockerfile targets node:22-alpine.",
  "engines": {
    "node": ">=20 <23"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "uuid": "^11.1.0"
  }
}
