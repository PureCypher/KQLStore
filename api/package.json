{
  "name": "kqlstore-api",
  "version": "1.0.0",
  "description": "KQL Store API server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "// engines": "better-sqlite3 11.x ships prebuilds only up to the Node 22 ABI, so Node 24 compiles it from source instead — that works and is what api/Dockerfile does. Node 26 is excluded because 11.x does not compile against its V8 headers at all. The Dockerfile targets node:24-alpine (Active LTS).",
  "engines": {
    "node": ">=20 <25"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "uuid": "^14.0.1"
  }
}
