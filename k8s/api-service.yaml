apiVersion: v1
kind: Service
metadata:
  name: kqlstore-api
  namespace: kqlstore
  labels:
    app: kqlstore-api
    app.kubernetes.io/managed-by: kustomize
spec:
  type: ClusterIP
  selector:
    app: kqlstore-api
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
