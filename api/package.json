{
  "name": "kqlstore-api",
  "version": "1.0.0",
  "description": "KQL Store API server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "// engines": "better-sqlite3 13.x ships ABI-independent N-API prebuilds, so the runtime major no longer decides whether the addon works — it runs on 20 through 26 alike. The upper bound is a release-line choice, not a dependency constraint: the Dockerfile targets node:24-alpine because 24 is the Active LTS, and 26 is Current until 2026-10-28.",
  "engines": {
    "node": ">=20 <25"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.2",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "uuid": "^14.0.1"
  }
}
